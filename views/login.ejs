<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>SparkBlog - Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    .slideshow-container {
      position: relative;
      height: 100vh;
      overflow: hidden;
    }

    .slideshow-bg {
      position: absolute;
      top: 0;
      left: 0;
      height: 100%;
      width: 100%;
      background-size: cover;
      background-position: center;
      transition: opacity 2s ease-in-out;
      opacity: 0;
    }

    .slideshow-bg.active {
      opacity: 1;
      z-index: 1;
    }

    .login-content {
      position: relative;
      z-index: 2;
    }
  </style>
</head>
<body class="bg-zinc-900 text-white">

  <!-- Navbar -->
  <nav class="bg-zinc-800 px-6 py-4 shadow-md flex justify-between items-center">
    <h1 class="text-3xl font-bold text-cyan-400">SparkBlog</h1>
    <div class="space-x-4">
      <a href="/login" class="text-white bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded">Login</a>
      <a href="/register" class="text-white bg-green-600 hover:bg-green-700 px-4 py-2 rounded">Register</a>
    </div>
  </nav>

  <!-- Slideshow + Login -->
  <section class="slideshow-container flex items-center justify-center">
    <!-- Backgrounds -->
    <div class="slideshow-bg active" style="background-image: url('https://images.unsplash.com/photo-1524995997946-a1c2e315a42f?auto=format&fit=crop&w=1950&q=80');"></div>
    <div class="slideshow-bg" style="background-image: url('https://images.unsplash.com/photo-1504384308090-c894fdcc538d?auto=format&fit=crop&w=1950&q=80');"></div>
    <div class="slideshow-bg" style="background-image: url('https://images.unsplash.com/photo-1521737604893-d14cc237f11d?auto=format&fit=crop&w=1950&q=80');"></div>

    <!-- Login Form -->
    <div class="login-content bg-black bg-opacity-60 p-10 rounded-lg shadow-lg w-full max-w-md">
      <h2 class="text-3xl font-bold text-cyan-300 mb-6 text-center">Login to SparkBlog</h2>
      <form action="/login" method="POST" class="space-y-6">
        <div>
          <label for="email" class="block text-gray-300 mb-2">Email</label>
          <input type="email" name="email" id="email" required
                 class="w-full px-4 py-2 bg-zinc-800 border border-gray-600 rounded text-white focus:outline-none focus:border-cyan-400" />
        </div>
        <div>
          <label for="password" class="block text-gray-300 mb-2">Password</label>
          <input type="password" name="password" id="password" required
                 class="w-full px-4 py-2 bg-zinc-800 border border-gray-600 rounded text-white focus:outline-none focus:border-cyan-400" />
        </div>
        <button type="submit"
                class="w-full bg-cyan-500 hover:bg-cyan-600 text-white py-2 rounded text-lg">Login</button>
        <p class="text-center text-sm text-gray-400 mt-4">
          Don't have an account?
          <a href="/register" class="text-cyan-400 hover:underline">Register here</a> <br>
          <a href="/" class="text-cyan-400 hover:underline">Back to home</a>
        </p>
      </form>
    </div>
  </section>

  <!-- Slideshow Script -->
  <script>
    const slides = document.querySelectorAll(".slideshow-bg");
    let current = 0;

    setInterval(() => {
      slides[current].classList.remove("active");
      current = (current + 1) % slides.length;
      slides[current].classList.add("active");
    }, 5000);
  </script>
</body>
</html>